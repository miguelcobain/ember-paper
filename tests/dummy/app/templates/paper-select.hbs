{{#paper-toolbar}}
    <h2 class="md-toolbar-tools">
      {{#paper-sidenav-toggle class="menu-sidenav-toggle"}}
        {{paper-icon icon="menu"}}
      {{/paper-sidenav-toggle}}
      <span>Select</span>
    </h2>
{{/paper-toolbar}}

{{#paper-content classNames="md-padding"}}
<div class="doc-content">
<h2>Basic Usage</h2>


<h1 class="md-title">Enter an address</h1>
<div layout="row">
    {{paper-input label="Street Name" value=street}}
    {{paper-input label="City" value=city}}
    {{#paper-select disabled=disableSelect placeholder="State" model=userState}}
      {{#each states as |state|}}
        {{#paper-option value=state.abbrev}}
          {{state.abbrev}}
        {{/paper-option}}
      {{/each}}
    {{/paper-select}}
</div>

<p>
  {{paper-checkbox label="Disable select" checked=disableSelect}}
</p>
<h3>Template</h3>
{{#code-block language='handlebars'}}\{{#paper-select disabled=disableSelect placeholder="State" model=userState}}
  \{{#each states as |state|}}
    \{{#paper-option value=state.abbrev}}
      \{{state.abbrev}}
    \{{/paper-option}}
  \{{/each}}
\{{/paper-select}}{{/code-block}}


<h2>Option groups</h2>


<h1 class="md-title">Pick your pizza below</h1>
<div layout="row">
    <md-input-container>
      {{#paper-select placeholder="Size" model=pizzaSize}}
        {{#each sizes as |size|}}
          {{#paper-option value=size}}
            {{size}}
          {{/paper-option}}
        {{/each}}
      {{/paper-select}}
    </md-input-container>
    <md-input-container>
      {{#paper-select placeholder="Topping" model=topping}}
        {{#paper-optgroup label="Meats"}}
          {{#each meatToppings as |topping|}}
            {{#paper-option value=topping.name}}
              {{topping.name}}
            {{/paper-option}}
          {{/each}}
        {{/paper-optgroup}}
        {{#paper-optgroup label="Veg"}}
          {{#each vegToppings as |topping|}}
            {{#paper-option value=topping.name}}
              {{topping.name}}
            {{/paper-option}}
          {{/each}}
        {{/paper-optgroup}}
      {{/paper-select}}
    </md-input-container>
</div>


<h3>Template</h3>
{{#code-block language='handlebars'}}\{{#paper-select placeholder="Topping" model=topping}}
  \{{#paper-optgroup label="Meats"}}
    \{{#each meatToppings as |topping|}}
      \{{#paper-option value=topping.name}}
        \{{topping.name}}
      \{{/paper-option}}
    \{{/each}}
  \{{/paper-optgroup}}
  \{{#paper-optgroup label="Veg"}}
    \{{#each vegToppings as |topping|}}
      \{{#paper-option value=topping.name}}
        \{{topping.name}}
      \{{/paper-option}}
    \{{/each}}
  \{{/paper-optgroup}}
\{{/paper-select}}{{/code-block}}



<h2>Options with async search</h2>


<div layout="column" layout-align="center center">
  <p>Select can call an arbitrary function on show. If this function returns a promise, it will display a loading indicator while it is being resolved:</p>
  <div layout="column" layout-align="center center" style="height: 100px;">
    {{#paper-select on-open=onUserLoad item-label-callback=userLabelCallback placeholder="Assign to a user" model=selectedUser as |items|}}
      {{#each items as |user|}}
        {{#paper-option value=user}}
          {{user.name}}
        {{/paper-option}}
      {{/each}}
    {{/paper-select}}

    {{#if selectedUser}}
      <p>You have selected the user <code>{{selectedUser.name}}</code></p>
    {{/if}}
  </div>
</div>


<h3>Template</h3>
<p>
    We provide two callbacks to the paper-select component <code class="paper">on-open</code> and <code class="paper">item-label-callback</code>.
    The item-label-callback will resolve the label of the object's given from the <code class="paper">on-open</code> callback.
</p>
{{#code-block language='handlebars'}}\{{#paper-select on-open=onUserLoad item-label-callback=userLabelCallback placeholder="Assign to a user" model=selectedUser as |items|}}
  \{{#each items as |user|}}
    \{{#paper-option value=user}}
      \{{user.name}}
    \{{/paper-option}}
  \{{/each}}
\{{/paper-select}}{{/code-block}}


</div>
{{/paper-content}}
