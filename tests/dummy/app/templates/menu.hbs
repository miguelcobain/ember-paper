{{#paper-toolbar}}
  <div class="md-toolbar-tools">
    {{#paper-sidenav-toggle as |toggle|}}
      {{#paper-button target=toggle action="toggleMenu" icon-button=true}}{{paper-icon icon="menu"}}{{/paper-button}}
    {{/paper-sidenav-toggle}}
    <span class="md-breadcrumb-page">Menu</span>
  </div>
{{/paper-toolbar}}

{{#paper-content classNames="md-padding"}}
<div class="doc-content">


{{#paper-card}}
{{#paper-card-content}}
<h2>Simple dropdown menu</h2>

  <p>Note that applying the class <code class="paper">.md-menu-origin</code> and <code class="paper">md-menu-align-target</code>
      attributes ensure that the menu elements align</p>

  <div layout-align="center center" layout="column">
      {{#paper-menu width=4 as |menu|}}
        {{#paper-button target=menu action="toggleMenu" icon-button=true}}{{paper-icon class="md-menu-origin" icon="local-phone"}}{{/paper-button}}
      {{else}}
        {{#each items as |item|}}
          {{#paper-menu-item action="openSomething"}}
            {{paper-icon icon=item.icon class="md-menu-align-target"}}
            <span>{{item.title}}</span>
          {{/paper-menu-item}}
        {{/each}}

        {{#paper-menu-item action="openSomething" disabled=true}}
          {{paper-icon icon="adb" class="md-menu-align-target"}}
            <span>This is disabled.</span>
        {{/paper-menu-item}}

        {{paper-divider}}

        {{#each items as |item|}}
          {{#paper-menu-item action="openSomething"}}
            {{paper-icon icon=item.icon class="md-menu-align-target"}} {{item.title}}
          {{/paper-menu-item}}
        {{/each}}
      {{/paper-menu}}
</div>

<h2>Template</h2>

{{#code-block language='handlebars'}}\{{#paper-menu as |menu|}}
  \{{#paper-button target=menu action="toggleMenu" icon-button=true}}
      \{{paper-icon class="md-menu-origin" icon="local-phone"}}
  \{{/paper-button}}
\{{else}}
  \{{#each items as |item|}}
    \{{#paper-menu-item action="openSomething"}}
      \{{paper-icon icon=item.icon class="md-menu-align-target"}}
      &lt;span&gt;\{{item.title}}&lt;/span&gt;
    \{{/paper-menu-item}}
  \{{/each}}
\{{/paper-menu}}{{/code-block}}

{{/paper-card-content}}
{{/paper-card}}





{{#paper-card}}
{{#paper-card-content}}
<h2>Menu Position Modes</h2>

<p>The <code class="paper">position</code> attribute can be used to specify the positioning along the
    <code class="paper">x</code> and <code class="paper">y</code> axis.</p>

<div layout-align="center center" layout="column">
    <div class="menus" layout-fill="" layout-wrap="" layout="row" layout-align="space-between center">
        <div layout="column" flex="33" flex-sm="100" layout-align="center center">
            <p>Target Mode Positioning (default)</p>
            {{#paper-menu is-open=menuTwoIsOpen as |menu|}}
              {{#paper-button target=menu action="toggleMenu" icon-button=true}}{{paper-icon class="md-menu-origin" icon="local-phone"}}{{/paper-button}}
            {{else}}
              {{#each items as |item|}}
                {{#paper-menu-item action="openSomething"}}
                  {{paper-icon icon=item.icon class="md-menu-align-target"}}
                  <span>{{item.title}}</span>
                {{/paper-menu-item}}
              {{/each}}
            {{/paper-menu}}
        </div>
        <div layout="column" flex="33" flex-sm="100" layout-align="center center">
            <p>Target mode with <code class="paper">md-offset</code></p>
            {{#paper-menu is-open=menuThreeIsOpen offset="0 -5" as |menu|}}
              {{#paper-button target=menu action="toggleMenu" icon-button=true}}{{paper-icon class="md-menu-origin" icon="local-phone"}}{{/paper-button}}
            {{else}}
              {{#each items as |item|}}
                {{#paper-menu-item action="openSomething"}}
                  {{paper-icon icon=item.icon class="md-menu-align-target"}}
                  <span>{{item.title}}</span>
                {{/paper-menu-item}}
              {{/each}}
            {{/paper-menu}}
        </div>
        <div layout="column" flex="33" flex-sm="100" layout-align="center center">
            <p><code class="paper">md-position-mode="target-right target"</code></p>
            {{#paper-menu is-open=menuFourIsOpen position="target-right target" as |menu|}}
              {{#paper-button target=menu action="toggleMenu" icon-button=true}}{{paper-icon class="md-menu-origin" icon="local-phone"}}{{/paper-button}}
            {{else}}
              {{#each items as |item|}}
                {{#paper-menu-item action="openSomething"}}
                    <p style="width: 190px;">{{item.title}}</p>
                    {{paper-icon icon=item.icon class="md-menu-align-target"}}
                {{/paper-menu-item}}
              {{/each}}
            {{/paper-menu}}

        </div>
    </div>
</div>




<h3>Template</h3>
<p>Use the <code class="paper">position</code> attribute to set where the menu should appear. The position attribute
should be used together with <code class="paper">.md-menu-origin</code> and <code class="paper">.md-menu-align-target</code>
attributes to tell where it should position itself in respect to origin and align-target.
</p>
{{#code-block language='handlebars'}}\{{#paper-menu position="target-right target" as |menu|}}
  \{{#paper-button target=menu action="toggleMenu" icon-button=true}}\{{paper-icon class="md-menu-origin" icon="local-phone"}}\{{/paper-button}}
\{{else}}
  \{{#paper-menu-item action="openSomething"}}
      &lt;p style="width: 190px;"&gt;Some title&lt;/p&gt;
    \{{paper-icon icon="adb" class="md-menu-align-target"}}
  \{{/paper-menu-item}}
\{{/paper-menu}}{{/code-block}}

{{/paper-card-content}}
{{/paper-card}}



{{#paper-card}}
{{#paper-card-content}}
<h2>Different Widths</h2>

<p><code class="paper">paper-menu-content</code> may specify a width attribute which will follow the
    <a href="http://www.google.com/design/spec/components/menus.html#menus-specs">official spec</a>.
</p>

<div layout-align="center center" layout="column">
    <div class="menus" layout-fill="" layout-wrap="" layout="row" layout-align="space-between center">
        <div layout="column" flex="33" flex-sm="100" layout-align="center center">
            <p>Wide menu (<code class="paper">width=6</code>)</p>
            {{#paper-menu width=6 as |menu|}}
              {{#paper-button target=menu action="toggleMenu" icon-button=true}}{{paper-icon class="md-menu-origin" icon="local-phone"}}{{/paper-button}}
            {{else}}
              {{#each options as |item|}}
                {{#paper-menu-item action="openSomething"}}
                    <span class="md-menu-align-target">Option</span> {{item}}
                {{/paper-menu-item}}
              {{/each}}
            {{/paper-menu}}
        </div>
        <div layout="column" flex="33" flex-sm="100" layout-align="center center">
            <p>Medium menu (<code class="paper">width=4</code>)</p>
            {{#paper-menu width=4 offset="0 -5" as |menu|}}
              {{#paper-button target=menu action="toggleMenu" icon-button=true}}{{paper-icon class="md-menu-origin" icon="local-phone"}}{{/paper-button}}
            {{else}}
              {{#each options as |item|}}
                {{#paper-menu-item action="openSomething"}}
                    <span class="md-menu-align-target">Option</span> {{item}}
                {{/paper-menu-item}}
              {{/each}}
            {{/paper-menu}}
        </div>
        <div layout="column" flex="33" flex-sm="100" layout-align="center center">
            <p>Small menu (<code class="paper">width=2</code>)</p>
            {{#paper-menu width=2 position="target-right target" as |menu|}}
              {{#paper-button target=menu action="toggleMenu" icon-button=true}}{{paper-icon class="md-menu-origin" icon="local-phone"}}{{/paper-button}}
            {{else}}
              {{#each options as |item|}}
                {{#paper-menu-item action="openSomething"}}
                    <span class="md-menu-align-target">Option</span> {{item}}
                {{/paper-menu-item}}
              {{/each}}
            {{/paper-menu}}
        </div>
    </div>
</div>



<h3>Template</h3>
{{#code-block language='handlebars'}}...
\{{#paper-menu width=4}}
  ...
\{{/paper-menu}}
...{{/code-block}}

{{/paper-card-content}}
{{/paper-card}}

</div>
{{/paper-content}}
