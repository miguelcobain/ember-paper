import actions from 'dummy/utils/actions';
import { module, test } from 'qunit';

module('Unit | Utility | actions', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = actions();
    assert.ok(result);
  });
});
