{{! template-lint-disable no-implicit-this }}
{{#in-element destinationEl insertBefore=null}}
  <PaperToastInner @swipeToClose={{swipeToClose}} @onClose={{onClose}} @top={{top}} @left={{left}} @capsule={{capsule}} @class={{class}}>
    {{yield (hash
      text=(component "paper-toast-text")
    )}}
  </PaperToastInner>
{{/in-element}}
