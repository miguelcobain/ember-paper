{{#ember-wormhole to=destinationId}}
  {{#paper-toast-inner swipe=swipeAction swipeToClose=swipeToClose onClose=onClose top=top left=left capsule=capsule class=className}}
    {{yield (hash
      text=(component "paper-toast-text")
    )}}
  {{/paper-toast-inner}}
{{/ember-wormhole}}
