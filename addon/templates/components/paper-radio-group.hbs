{{yield (hash
  radio=(component radioComponent
    toggle=toggle
    disabled=disabled
    groupValue=groupValue
    onChange=(action "onChange")
    parentComponent=this
    shouldRegister=true)
)}}
