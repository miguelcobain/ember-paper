{{yield (hash
  radio=(component "paper-radio"
    toggle=toggle
    disabled=disabled
    groupValue=groupValue
    onChange=(action "onChange")
    parentComponent=this
    shouldRegister=true)
)}}
