{{#if label}}
  <label class={{if value 'md-static' 'md-placeholder'}} for={{inputElementId}}>{{label}}</label>
{{/if}}

{{#if icon}}
  {{paper-icon icon class=icon-class}}
{{/if}}

{{#paper-select-core placeholder=placeholder value=value disabled=disabled on-open=onOpen item-label-callback=itemLabelCallback as |items|}}
  {{yield items}}
{{/paper-select-core}}
