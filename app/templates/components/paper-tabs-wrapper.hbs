{{#if shouldPaginate}}
  {{paper-prev-button parent=this}}
  {{paper-next-button parent=this}}
{{/if}}
<md-tabs-canvas class="{{if shouldPaginate 'md-paginated'}}">
  {{#paper-pagination-wrapper parent=this}}
    {{yield}}
  {{/paper-pagination-wrapper}}
</md-tabs-canvas>
