{{#if noSpan}}
  {{#if hasBlock}}
    {{yield}}
  {{else}}
    {{label}}
  {{/if}}
{{else}}
  <span type={{type}} disabled={{disabled}}>
    {{#if hasBlock}}
      {{yield}}
    {{else}}
      {{label}}
    {{/if}}
  </span>
{{/if}}