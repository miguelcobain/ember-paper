{{#if label}}
  {{#paper-tab-label}}{{label}}{{/paper-tab-label}}
  {{#paper-tab-body parent=this}}
    {{yield}}
  {{/paper-tab-body}}
{{else}}
  {{yield (hash
    label=(component
      'paper-tab-label')
    body=(component
      'paper-tab-body'
      parent=this)
  )}}
{{/if}}
